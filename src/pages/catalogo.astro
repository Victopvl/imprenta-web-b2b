---
// src/pages/catalogo.astro
import Layout from '../layouts/Layout.astro';

const catalogData = [
  {
    id: "copiado-impresion",
    titulo: "Copiado e Impresión",
    descripcion: "Soluciones de alta fidelidad, offset y digital optimizadas para altos volúmenes corporativos.",
    productos: [
      { nombre: "Carpeta Corporativa", sustrato: "Couché 350g / Opaco / Barniz Sectorizado", moq: "500 unds." },
      { nombre: "Hojas Membretadas", sustrato: "Hilado 90g / Láser Premium / Pantone Exacto", moq: "1.000 unds." },
      { nombre: "Sobres Institucionales", sustrato: "Hilado 120g (Oficio / Americano) con Sello", moq: "1.000 unds." },
      { nombre: "Fotocopias e Impresiones Masivas", sustrato: "Papel Multipropósito 75g / 80g en Línea", moq: "5.000 págs." }
    ]
  },
  {
    id: "regalos-corporativos",
    titulo: "Regalos Corporativos",
    descripcion: "Líneas de merchandising premium e institucional con sistemas de grabado indeleble.",
    productos: [
      { nombre: "Libretas Personalizadas", sustrato: "Tapa Dura PU / Eco-cuero / Cierre Elástico", moq: "100 unds." },
      { nombre: "Bolígrafos Ejecutivos", sustrato: "Aluminio Anodizado / Grabado Láser Fibra", moq: "200 unds." },
      { nombre: "Botellas y Mugs Térmicos", sustrato: "Acero Inoxidable Doble Pared / Térmico", moq: "100 unds." }
    ]
  },
  {
    id: "otros-servicios",
    titulo: "Otros Servicios",
    descripcion: "Terminaciones especiales y packaging a medida bajo demanda industrial.",
    productos: [
      { nombre: "Cajas Autoarmables", sustrato: "Cartón Corrugado / Impresión Flexográfica", moq: "300 unds." },
      { nombre: "Etiquetas Adhesivas", sustrato: "Ilustración / Polipropileno Transparente en Rollo", moq: "2.500 unds." }
    ]
  }
];
---

<Layout title="Catálogo Técnico de Productos">
  
  <section class="bg-gradient-to-br from-[#009EE0] via-[#007BB3] to-[#00456F] relative overflow-hidden py-24 md:py-32 text-center">
    <div class="absolute inset-0 bg-[radial-gradient(#91DAEE_1px,transparent_1px)] [background-size:16px_16px] opacity-15"></div>
    <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10 text-center">
      <span class="text-[#91DAEE] font-bold uppercase tracking-widest text-xs">Suministro y Abastecimiento B2B</span>
      <h1 class="text-4xl md:text-5xl lg:text-6xl font-black text-white mt-3 mb-6 tracking-tight">
        Catálogo Técnico de Productos
      </h1>
      <p class="text-blue-50/90 font-light leading-relaxed text-base md:text-lg">
        Consulte especificaciones de materiales, acabados de protección y volúmenes mínimos (MOQ) de nuestra planta de producción gráfica.
      </p>
    </div>
  </section>

  <section class="py-12 md:py-16 bg-[#F7FAFD]">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      
      <div class="bg-white border border-[#91DAEE]/40 rounded-2xl p-6 mb-16 shadow-xl shadow-blue-900/5 -translate-y-12 relative z-20">
        <div class="flex flex-col lg:flex-row gap-5 items-center justify-between">
          <div class="w-full lg:max-w-md relative">
            <span class="absolute inset-y-0 left-0 pl-3.5 flex items-center text-slate-400">🔍</span>
            <input 
              type="text" 
              id="search-input" 
              placeholder="Buscar por palabra clave (ej: carpeta, papel)..." 
              class="w-full bg-white border border-slate-300 rounded-xl pl-10 pr-4 py-3.5 text-sm text-[#00456F] font-medium placeholder-slate-400 outline-none focus:border-[#009EE0] focus:ring-2 focus:ring-[#009EE0]/20 transition-all shadow-sm"
            />
          </div>
          
          <div class="flex flex-wrap gap-2 w-full lg:w-auto" id="category-filters">
             <button data-target="all" class="category-btn text-[11px] font-bold uppercase tracking-wider px-5 py-3 rounded-xl bg-[#009EE0] text-white transition-all shadow-md">
               Todos los productos
             </button>
             {catalogData.map((cat) => (
               <button data-target={cat.id} class="category-btn text-[11px] font-bold uppercase tracking-wider px-5 py-3 rounded-xl bg-[#E2E8F0] text-[#00456F] hover:bg-[#91DAEE]/30 transition-all font-semibold">
                 {cat.titulo}
               </button>
             ))}
          </div>
        </div>
      </div>

      <div class="space-y-24" id="catalog-sections-container">
        {catalogData.map((category) => (
          <div class="category-section transition-all duration-300" data-category={category.id}>
            
            <div class="mb-6">
              <div class="flex items-center gap-3 mb-2">
                <div class="w-3 h-7 bg-[#009EE0] rounded-md"></div>
                <h2 class="text-[#00456F] font-black tracking-tight text-2xl md:text-3xl">{category.titulo}</h2>
              </div>
              <p class="text-slate-600 font-normal leading-relaxed text-sm">{category.descripcion}</p>
            </div>

            <div class="bg-white rounded-2xl border border-[#91DAEE]/30 shadow-sm overflow-hidden">
              <div class="overflow-x-auto">
                <table class="w-full text-left text-sm border-collapse">
                  <thead class="bg-[#F7FAFD] text-[#00456F] font-bold uppercase text-[11px] tracking-wider border-b border-[#91DAEE]/30">
                    <tr>
                      <th class="px-6 py-4 md:px-8 md:py-5">Ítem / Solución</th>
                      <th class="px-6 py-4 md:px-8 md:py-5">Material / Sustrato Base</th>
                      <th class="px-6 py-4 md:px-8 md:py-5 text-right">Mínimo Comercial (MOQ)</th>
                    </tr>
                  </thead>
                  <tbody class="divide-y divide-[#91DAEE]/15">
                    {category.productos.map((p) => (
                      <tr class="product-row hover:bg-[#F7FAFD]/70 transition-colors">
                        <td class="px-6 py-4 md:px-8 md:py-5 font-bold text-[#00456F] text-base">{p.nombre}</td>
                        <td class="px-6 py-4 md:px-8 md:py-5 text-slate-600 font-normal">{p.sustrato}</td>
                        <td class="px-6 py-4 md:px-8 md:py-5 text-right">
                          <span class="inline-block font-mono font-bold bg-[#F7FAFD] text-[#009EE0] border border-[#91DAEE]/50 px-3 py-1 rounded-lg text-xs">
                            {p.moq}
                          </span>
                        </td>
                      </tr>
                    ))}
                  </tbody>
                </table>
              </div>
            </div>

          </div>
        ))}
      </div>

    </div>
  </section>

  <script is:inline>
    document.addEventListener('DOMContentLoaded', () => {
      const searchInput = document.getElementById('search-input');
      const filterButtons = document.querySelectorAll('.category-btn');
      const sections = document.querySelectorAll('.category-section');
      let currentCategory = 'all';

      function filterCatalog() {
        const query = searchInput.value.toLowerCase().trim();

        sections.forEach(section => {
          const catId = section.getAttribute('data-category');
          const matchesCategory = (currentCategory === 'all' || catId === currentCategory);
          
          const rows = section.querySelectorAll('.product-row');
          let visibleRowsInSection = 0;

          rows.forEach(row => {
            const text = row.textContent.toLowerCase();
            if (text.includes(query)) {
              row.style.display = '';
              visibleRowsInSection++;
            } else {
              row.style.display = 'none';
            }
          });

          if (matchesCategory && visibleRowsInSection > 0) {
            section.style.display = '';
          } else {
            section.style.display = 'none';
          }
        });
      }

      filterButtons.forEach(btn => {
        btn.addEventListener('click', () => {
          filterButtons.forEach(b => {
            b.classList.remove('bg-[#009EE0]', 'text-white', 'shadow-md');
            b.classList.add('bg-[#E2E8F0]', 'text-[#00456F]');
          });

          btn.classList.remove('bg-[#E2E8F0]', 'text-[#00456F]');
          btn.classList.add('bg-[#009EE0]', 'text-white', 'shadow-md');

          currentCategory = btn.getAttribute('data-target');
          filterCatalog();
        });
      });

      searchInput.addEventListener('input', filterCatalog);
    });
  </script>
</Layout>